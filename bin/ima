#!/bin/sh

if [ "x$(echo $OSTYPE | grep -i darwin)" != "x" ]; then
  # macos
  /bin/echo -n "$(date '+%Y-%m-%d(%a) %H:%M %Z') / $(date -u '+%Y-%m-%d(%a) %H:%M %Z')" | pbcopy
else
  # ubuntu on WSL
  echo "$(date '+%Y-%m-%d(%a) %H:%M %Z') / $(date -u '+%Y-%m-%d(%a) %H:%M %Z')" | clip.exe
fi

